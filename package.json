{
  "name": "airport-semey",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others-on-fail -n backend,frontend -c blue,green \"pnpm --dir backend start:dev\" \"npm --dir frontend run dev\"",
    "dev:backend": "pnpm --dir backend start:dev",
    "dev:frontend": "npm --dir frontend run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
